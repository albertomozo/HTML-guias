# Guía Rápida: Generar HTML con JavaScript 
## 🎯 Objetivo Aprender los 3 métodos principales para crear contenido HTML dinámicamente usando JavaScript y datos externos.
 ## 📊 Datos de Ejemplo 
 ```javascript // Array de objetos const productos = [ { id: 1, nombre: "Laptop", precio: 999, stock: 5 }, { id: 2, nombre: "Mouse", precio: 25, stock: 15 }, { id: 3, nombre: "Teclado", precio: 75, stock: 8 } ]; // JSON simulado (como si viniera de una API) const usuariosJSON = '[{"id":1,"nombre":"Ana","email":"ana@email.com"}]'; const usuarios = JSON.parse(usuariosJSON); ``` --- ## 1️⃣ MÉTODO: innerHTML ### ¿Qué hace? Reemplaza todo el contenido HTML de un elemento. ### Sintaxis Básica ```javascript elemento.innerHTML = "nuevo HTML"; ``` ### Ejemplo Paso a Paso ```javascript // 1. Obtener el contenedor const contenedor = document.getElementById('lista-productos'); // 2. Crear variable para acumular HTML let html = '
Productos Disponibles
'; // 3. Recorrer los datos y construir HTML productos.forEach(producto => { html += `
${producto.nombre}
Precio: $${producto.precio}

Stock: ${producto.stock}

`; }); // 4. Insertar todo el HTML de una vez contenedor.innerHTML = html; ``` ### ✅ Ventajas - **Fácil de entender** y escribir - **Template strings** permiten interpolación - **Perfecto para principiantes** ### ❌ Desventajas - **Reemplaza todo** el contenido existente - **Vulnerable a XSS** si usas datos no confiables - **Menos eficiente** en contenido grande --- ## 2️⃣ MÉTODO: createElement ### ¿Qué hace? Crea elementos HTML uno por uno de forma programática. ### Sintaxis Básica ```javascript const elemento = document.createElement('etiqueta'); elemento.textContent = 'contenido'; padre.appendChild(elemento); ``` ### Ejemplo Paso a Paso ```javascript // 1. Obtener el contenedor const contenedor = document.getElementById('lista-productos'); // 2. Limpiar contenido previo contenedor.innerHTML = ''; // 3. Crear título const titulo = document.createElement('h3'); titulo.textContent = 'Productos Disponibles'; contenedor.appendChild(titulo); // 4. Crear elementos para cada producto productos.forEach(producto => { // Crear contenedor del producto const divProducto = document.createElement('div'); divProducto.className = 'producto'; // Crear y configurar nombre const nombre = document.createElement('h4'); nombre.textContent = producto.nombre; // Crear precio const precio = document.createElement('p'); precio.textContent = `Precio: $${producto.precio}`; // Crear stock const stock = document.createElement('p'); stock.textContent = `Stock: ${producto.stock}`; // Crear botón con evento const boton = document.createElement('button'); boton.textContent = 'Comprar'; boton.onclick = () => alert(`Compraste: ${producto.nombre}`); // Ensamblar elemento divProducto.appendChild(nombre); divProducto.appendChild(precio); divProducto.appendChild(stock); divProducto.appendChild(boton); // Agregar al contenedor contenedor.appendChild(divProducto); }); ``` ### ✅ Ventajas - **Muy seguro** contra XSS - **Mejor rendimiento** en aplicaciones grandes - **Control total** sobre elementos y eventos - **Fácil debugging** ### ❌ Desventajas - **Más código** para escribir - **Curva de aprendizaje** mayor - **Menos legible** para HTML complejo --- ## 3️⃣ MÉTODO: insertAdjacentHTML ### ¿Qué hace? Inserta HTML en posiciones específicas sin afectar el contenido existente. ### Posiciones Disponibles ```javascript // 'beforebegin' - Antes del elemento // 'afterbegin' - Dentro del elemento, al inicio // 'beforeend' - Dentro del elemento, al final // 'afterend' - Después del elemento ``` ### Sintaxis Básica ```javascript elemento.insertAdjacentHTML('posicion', 'HTML'); ``` ### Ejemplo Paso a Paso ```javascript // 1. Obtener el contenedor const contenedor = document.getElementById('lista-productos'); // 2. Agregar título si no existe if (!contenedor.querySelector('h3')) { contenedor.insertAdjacentHTML('afterbegin', '
Productos Disponibles
'); } // 3. Agregar cada producto al final productos.forEach(producto => { const html = `
${producto.nombre}
Precio: $${producto.precio}

Stock: ${producto.stock}

`; contenedor.insertAdjacentHTML('beforeend', html); }); // 4. Agregar producto individual (ejemplo) function agregarProducto(nuevoProducto) { const html = `
${nuevoProducto.nombre}
Precio: $${nuevoProducto.precio}

¡NUEVO!

`; contenedor.insertAdjacentHTML('afterbegin', html); // Al inicio } ``` ### ✅ Ventajas - **Preserva** elementos existentes - **Control de posición** preciso - **Más eficiente** que innerHTML - **Fácil de usar** ### ❌ Desventajas - **Vulnerable a XSS** como innerHTML - **Menos control** que createElement --- ## 📋 Comparación Rápida | Aspecto | innerHTML | createElement | insertAdjacentHTML | |---------|-----------|---------------|-------------------| | **Facilidad** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ | | **Seguridad** | ⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐ | | **Rendimiento** | ⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | | **Flexibilidad** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | --- ## 🎯 Cuándo Usar Cada Método ### 🔧 innerHTML ```javascript **✅ USAR para:** - Contenido inicial de la página - Prototipos rápidos - Datos confiables (no input del usuario) - Estudiantes principiantes **Ejemplo típico:** ```javascript function mostrarDashboard(datos) { const dashboard = document.getElementById('dashboard'); dashboard.innerHTML = `
Bienvenido ${datos.usuario}
Tienes ${datos.mensajes} mensajes nuevos

`; } ``` ``` ### ⚡ createElement ```javascript **✅ USAR para:** - Aplicaciones de producción - Elementos con eventos complejos - Máxima seguridad - Mejor rendimiento **Ejemplo típico:** ```javascript function crearFormulario() { const form = document.createElement('form'); const input = document.createElement('input'); const button = document.createElement('button'); input.type = 'text'; input.placeholder = 'Ingresa tu nombre'; button.textContent = 'Enviar'; button.onclick = (e) => { e.preventDefault(); console.log(input.value); }; form.appendChild(input); form.appendChild(button); return form; } ``` ``` ### 📝 insertAdjacentHTML ```javascript **✅ USAR para:** - Agregar contenido sin borrar existente - Actualizaciones incrementales - Control de posición específica **Ejemplo típico: Chat en tiempo real** ```javascript function agregarMensaje(mensaje) { const chat = document.getElementById('chat'); chat.insertAdjacentHTML('beforeend', `
${mensaje.autor}: ${mensaje.texto}
`); } ``` ``` --- ## 🛡️ Consejos de Seguridad ### ⚠️ Problema: XSS (Cross-Site Scripting) ```javascript // ❌ PELIGROSO - nunca hagas esto con datos del usuario: const userInput = ''; div.innerHTML = userInput; // ¡Ejecutará el script! // ✅ SEGURO - opciones: // Opción 1: Usar textContent div.textContent = userInput; // Solo texto, no HTML // Opción 2: Escapar HTML function escapeHtml(text) { const div = document.createElement('div'); div.textContent = text; return div.innerHTML; } // Opción 3: Usar createElement (más seguro) const p = document.createElement('p'); p.textContent = userInput; // Siempre seguro ``` --- ## 🏃‍♂️ Ejercicios Prácticos ### Ejercicio 1: Lista de Tareas (innerHTML) ```javascript const tareas = [ { id: 1, texto: "Estudiar JavaScript", completada: false }, { id: 2, texto: "Hacer ejercicios", completada: true } ]; // Tu código aquí: crear lista con innerHTML ``` ### Ejercicio 2: Calculadora (createElement) ```javascript // Tu código aquí: crear calculadora con botones usando createElement ``` ### Ejercicio 3: Comentarios en Blog (insertAdjacentHTML) ```javascript // Tu código aquí: sistema para agregar comentarios sin borrar existentes ``` --- ## 🎓 Resumen para Estudiantes 1. **Empieza con innerHTML** - Es lo más fácil para aprender 2. **Practica createElement** - Cuando necesites más control 3. **Usa insertAdjacentHTML** - Para actualizaciones incrementales 4. **Siempre valida datos** - Especialmente input del usuario 5. **Piensa en rendimiento** - createElement es mejor para apps grandes ### Progresión de Aprendizaje Recomendada: 1. **Semana 1-2**: innerHTML + template strings 2. **Semana 3-4**: createElement básico 3. **Semana 5**: insertAdjacentHTML 4. **Semana 6**: Comparación y casos de uso 5. **Semana 7**: Seguridad y buenas prácticas